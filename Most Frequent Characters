# Write a program that lets the user enter a string and displays the letter that appears most frequently in the
# string. Ignore spaces, punctuation, and uppercase vs lowercase.


def main():

    my_string = input('Please enter a phrase: ').upper()
    if my_string.isdigit():
        print("A", my_string, "appears")
    print()


main()
